---
description: Exercise RLM over a codebase/files context with repl_eval helpers
argument-hint: "[task override]"
---
Use the `rlm` tool with:

- `task`: `This is a codebase context. Use repl_eval if helpful. Count TypeScript files and name two under extensions/rlm. $@`
- `contextKind`: `files`
- `contextPath`: `extensions/rlm`
- `mode`: `auto`

Then report the final answer and mention whether `repl_eval` was used.
