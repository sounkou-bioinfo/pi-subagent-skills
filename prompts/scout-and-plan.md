---
description: Scout a codebase and turn the findings into a concrete plan
argument-hint: "<task>"
---
Use the `subagent` tool in a chain:

1. `scout` to investigate the repo and gather exact files/line ranges.
2. `planner` to turn those findings into a concrete file-by-file plan.

Task: $@
